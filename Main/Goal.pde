class Goal {
  int x, y;

  Goal(int xPos, int yPos) {
    x = xPos;
    y = yPos;
  }

  //--------------------------------------------------------------------------------------------------------------------------------
  void drawGoal() {
    fill(255, 0, 0);
    stroke(255, 0, 0);
    ellipse(x, y, 10, 10);
  }
}
